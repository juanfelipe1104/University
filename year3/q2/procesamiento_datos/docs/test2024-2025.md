# Test Procesamiento de Datos — Preguntas respondidas

## Pregunta 1

En Spark Structured Streaming, ¿qué representan la Input Table y la Result Table?

- Opción A: La Input Table almacena solo los datos nuevos, y la Result Table almacena una copia exacta del DataFrame original.
- **Opción B: La Input Table representa todos los datos recibidos desde la fuente hasta el momento, y la Result Table refleja el resultado actual de la consulta sobre esos datos. (Correcta)**
- Opción C: La Input Table contiene todos los resultados emitidos, y la Result Table almacena únicamente los datos eliminados del flujo.
- Opción D: La Input Table y la Result Table son tablas físicas creadas en HDFS para almacenar resultados intermedios.

---

## Pregunta 2

¿Qué representa un Dataset en Apache Spark y cómo se relaciona con los paradigmas de programación funcional y relacional?

- Opción A: Un Dataset es un tipo obsoleto que fue reemplazado completamente por RDDs en Spark 3.
- Opción B: Un Dataset es una colección mutable de datos sin tipado que solo se puede consultar con SQL.
- Opción C: Un Dataset es un conjunto de archivos JSON que solo pueden manipularse desde la consola de Spark.
- **Opción D: Un Dataset es una estructura distribuida y fuertemente tipada que permite combinar operaciones funcionales, como map y filter, con expresiones relacionales, como select y groupBy. (Correcta)**

---

## Pregunta 3

¿Cuál es el propósito de la operación `sc.parallelize` en Apache Spark?

- Opción A: Ejecutar una acción sobre un DataFrame.
- **Opción B: Crear un RDD a partir de una colección local. (Correcta)**
- Opción C: Convertir un DataFrame en un archivo CSV.
- Opción D: Transformar un RDD en una colección local.

---

## Pregunta 4

¿Qué efecto tiene iniciar un consumidor de Kafka con la opción `--from-beginning`?

- **Opción A: Hace que el consumidor lea todos los mensajes almacenados en el topic desde su creación, no solo los nuevos. (Correcta)**
- Opción B: Indica a Kafka que cree un nuevo topic desde cero antes de consumir mensajes.
- Opción C: Permite consumir solo los mensajes más recientes del topic, ignorando los antiguos.
- Opción D: Fuerza al consumidor a consumir solo los mensajes no confirmados por otros consumer groups.

---

## Pregunta 5

¿Qué es un transformador en Spark MLlib y qué obtenemos al aplicarlo?

- Opción A: Es una función que toma un modelo entrenado y lo convierte en una expresión lógica.
- Opción B: Es un modelo de aprendizaje automático que predice una etiqueta y devuelve una métrica de evaluación.
- Opción C: Es una operación que calcula automáticamente los hiperparámetros óptimos de un estimador.
- **Opción D: Es un componente que transforma un DataFrame, añadiendo o modificando columnas, y devuelve un nuevo DataFrame con esas transformaciones. (Correcta)**

---

## Pregunta 6

¿Cuál de las siguientes afirmaciones es cierta con respecto al planificador de trabajos de Apache Spark cuando se configura en modo FIFO o FAIR?

- Opción A: En el modo FAIR, los trabajos se ejecutan uno a la vez, respetando siempre el orden de llegada.
- Opción B: En el modo FIFO, los trabajos se ejecutan en el orden en que se envían, mientras que en el modo FAIR se priorizan los trabajos más largos para evitar el "starvation".
- Opción C: El modo FIFO garantiza que los trabajos se ejecuten en el orden en que llegan, sin tener en cuenta los recursos disponibles.
- **Opción D: El modo FAIR asigna recursos equitativamente entre trabajos, mientras que el modo FIFO ejecuta trabajos en el orden de llegada, sin considerar los recursos disponibles. (Correcta)**

---

## Pregunta 7

¿Cuál es la función principal del método `sc.textFile` en Apache Spark?

- Opción A: Ejecutar una consulta SQL sobre un RDD.
- Opción B: Convertir un DataFrame en un archivo CSV.
- Opción C: Escribir un RDD en un archivo de texto.
- **Opción D: Leer un archivo de texto y convertirlo en un RDD, línea por línea. (Correcta)**

---

## Pregunta 8

De acuerdo al siguiente código, ¿qué estructura programática de Spark, correspondiente con alguna de las APIs de la SparkSession, es la variable `data`? Razona tu respuesta.

```scala
data.filter(dato => dato.peso > 10)
```

- Opción A: SQL, porque dato es una tabla con datos estructurados.
- **Opción B: Dataset, porque se está realizando un filtro de acuerdo a un atributo de una clase. (Correcta)**
- Opción C: RDD, porque data contiene datos no estructurados.
- Opción D: Dataframe, porque la tabla dato tiene un campo llamado peso de tipo entero.

---

## Pregunta 9

¿Cuál es el resultado esperado del siguiente código?

```scala
val data = sc.parallelize(List(("a", 1), ("b", 2), ("a", 3), ("b", 4), ("c", 5)))
val zeroValue = ("", 0)

val resultado = data.aggregate(zeroValue)(
  (x, y) => if (y._2 % 2 == 0) (x._1.concat(y._1), x._2 + y._2) else x,
  (z, w) => (z._1.concat(w._1), z._2 + w._2)
)
```

- **Opción A: `resultado: (String, Int) = ("bb", 6)` (Correcta)**
- Opción B: `resultado: (Int, String) = (6, "bb")`
- Opción C: `resultado: Int = 6`
- Opción D: `resultado: Int = 9`

---

## Pregunta 10

¿Qué es y cómo funciona un consumer group en Apache Kafka?

- Opción A: Es un sistema de particionado que divide un topic en varias réplicas para mejorar la disponibilidad.
- Opción B: Es un conjunto de productores que escriben mensajes en un mismo topic de forma paralela.
- **Opción C: Es un conjunto de consumidores que colaboran para leer mensajes de un topic, asegurando que cada partición sea leída por un solo consumidor del grupo. (Correcta)**
- Opción D: Es una herramienta externa a Kafka que almacena mensajes procesados en un sistema de archivos distribuido.

---

## Pregunta 11

¿Qué son el underfitting y el overfitting en Machine Learning?

- **Opción A: Son tipos de errores que ocurren cuando el modelo es demasiado simple, underfitting, o demasiado complejo y se ajusta demasiado a los datos de entrenamiento, overfitting. (Correcta)**
- Opción B: Son algoritmos de clasificación binaria que utilizan árboles de decisión para corregir errores.
- Opción C: Son métricas estadísticas utilizadas para medir la precisión y la varianza del modelo.
- Opción D: Son técnicas de regularización que mejoran la capacidad de generalización del modelo.

---

## Pregunta 12

¿Cuál de las siguientes afirmaciones describe correctamente la diferencia entre un modelo supervisado y uno no supervisado en Machine Learning?

- Opción A: Ambos tipos de modelos necesitan etiquetas para poder entrenarse correctamente, pero el no supervisado se aplica a textos.
- Opción B: Un modelo supervisado detecta patrones en datos no etiquetados, mientras que uno no supervisado necesita etiquetas para entrenarse.
- **Opción C: Un modelo supervisado utiliza datos etiquetados para aprender una relación entre entradas y salidas; un modelo no supervisado encuentra estructuras ocultas en datos no etiquetados. (Correcta)**
- Opción D: Los modelos supervisados y no supervisados son equivalentes, pero se diferencian por la velocidad del algoritmo usado.

---

## Pregunta 13

Tienes un DataFrame con datos, ¿cómo guardarías esos datos en formato JSON en la ruta `/data/resultado` de HDFS?

- Opción A: `dataframe.collect().format("json").write("hdfs://localhost:9000/data/resultado")`
- Opción B: `dataframe.collect().format("json").write("/data/resultado")`
- **Opción C: `dataframe.write.json("hdfs://localhost:9000/data/resultado")` (Correcta)**
- Opción D: `dataframe.write.json("/data/resultado")`

---

## Pregunta 14

¿Qué es la técnica de checkpointing en Spark Streaming y cuál es su utilidad al trabajar con Apache Kafka?

- Opción A: Es una forma de almacenar todos los datos de Kafka en HDFS antes de iniciar el procesamiento.
- Opción B: Es una técnica para dividir un topic de Kafka en múltiples clústeres independientes para mejorar la paralelización.
- Opción C: Es un sistema de autenticación entre Spark y Kafka para asegurar conexiones seguras.
- **Opción D: Es el mecanismo por el cual Spark guarda de forma persistente el estado de las transformaciones y el progreso de lectura, permitiendo la recuperación tras fallos. (Correcta)**

---

## Pregunta 15

¿Cuáles son los tres tipos de semánticas de entrega en sistemas de streaming y cuál es la más adecuada para una transacción bancaria?

- Opción A: Exactamente una vez, aleatoria y asincrónica. Para banca se elige "asincrónica" porque prioriza velocidad sobre consistencia.
- **Opción B: Al menos una vez, como máximo una vez y exactamente una vez. Para una transacción bancaria se elige "exactamente una vez" para evitar duplicidades. (Correcta)**
- Opción C: Al menos una vez, al instante y en orden. Para transacciones bancarias se usa "en orden" para garantizar la secuencia de operaciones.
- Opción D: Directa, indirecta y paralela. Para banca se usa "directa" por ser la más rápida.

---

## Pregunta 16

¿Cuál es una de las principales ventajas de realizar procesos ETL en un entorno de sistemas distribuidos?

- Opción A: Los procesos ETL en sistemas distribuidos pueden ejecutarse sin dividir los datos entre nodos.
- Opción B: Los sistemas distribuidos eliminan la necesidad de transformar los datos antes de cargarlos.
- **Opción C: La capacidad de paralelizar tareas permite procesar grandes volúmenes de datos de forma más rápida y escalable. (Correcta)**
- Opción D: En sistemas distribuidos, los procesos ETL solo pueden realizarse sobre datos en tiempo real.

---

## Pregunta 17

¿Cuáles son los tres componentes principales de Apache Hadoop?

- Opción A: HDFS, Shuffle, Master
- Opción B: HDFS, MapReduce, Master
- Opción C: HDFS, Shuffle, YARN
- **Opción D: HDFS, MapReduce, YARN (Correcta)**

---

## Pregunta 18

En el contexto de Apache Spark, ¿qué son los metadatos y el esquema de una tabla?

- Opción A: Los metadatos se refieren a las operaciones de transformación aplicadas a los datos, y el esquema define la cantidad de nodos en el clúster.
- Opción B: Los metadatos son aquellos datos sobre los que se pueden aplicar filtros mientras que el esquema define únicamente el nombre de las columnas de una tabla.
- Opción C: Los metadatos son un tipo de archivo utilizado para almacenar los datos de forma comprimida, mientras que el esquema se refiere a las operaciones realizadas en la tabla.
- **Opción D: Los metadatos son toda información sobre una tabla, como la ubicación física de los datos, y el esquema, que forma parte de los metadatos, describe la estructura de los datos. (Correcta)**

---

## Pregunta 19

¿Cuál es la principal diferencia entre una transformación y una acción sobre un RDD en Apache Spark?

- **Opción A: Las transformaciones definen un plan de ejecución, evaluación diferida, mientras que las acciones desencadenan la ejecución real del RDD. (Correcta)**
- Opción B: Las transformaciones ejecutan inmediatamente los cálculos, mientras que las acciones se almacenan para después.
- Opción C: Las transformaciones eliminan datos duplicados, mientras que las acciones solo los agrupan.
- Opción D: Las acciones convierten un RDD en un DataFrame, mientras que las transformaciones convierten un DataFrame en un RDD.

---

## Pregunta 20

¿Cuáles son las tres características fundamentales conocidas como las “3 Vs” del Big Data?

- Opción A: Volumen, Valor y Visualización
- Opción B: Varianza, Valor y Velocidad
- **Opción C: Volumen, Velocidad y Variedad (Correcta)**
- Opción D: Velocidad, Veracidad y Validación
